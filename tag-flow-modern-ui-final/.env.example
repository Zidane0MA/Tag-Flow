# Ejemplo de configuración de variables de entorno para Tag-Flow Frontend

# =================================
# CONFIGURACIÓN DEL BACKEND FLASK
# =================================

# Host del servidor Flask (IP o hostname)
# Para desarrollo local: localhost
# Para acceso desde móvil en la misma red: IP local (ej: 192.168.1.135)
VITE_BACKEND_HOST=localhost

# Puerto del servidor Flask
VITE_BACKEND_PORT=5000

# =================================
# EJEMPLOS DE CONFIGURACIÓN
# =================================

# Desarrollo local (solo en la misma máquina):
# VITE_BACKEND_HOST=localhost
# VITE_BACKEND_PORT=5000

# Desarrollo con acceso móvil (red local):
# VITE_BACKEND_HOST=192.168.1.135
# VITE_BACKEND_PORT=5000

# Producción (automáticamente usa window.location.origin)
# No es necesario configurar estas variables en producción

# =================================
# VARIABLES LEGACY (NO USAR)
# =================================
# VITE_API_URL=http://localhost:5000